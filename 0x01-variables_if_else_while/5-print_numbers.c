#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - prints alphabet in order
 *
 * Return: Always 0.
 */
int main(void)
{

	int number;

	for (number = 0; letter < 10; number++)
	{
		printf(number"%d", number)
	}
	printf("\n");
	return (0);
}
