#include "main.h"

/**
 * print_square - prints a square of desire input
 * @size: size of square
 *
 * Return: void
 */


void print_diagonal(int n)
{
	int line, square;

	if (n <= 0)
		_putchar('\n');
	for (line = 0; line < size; line++)
	{
		for (square = 0; square < (size); b++)
		{
			_putchar('#');
		}
		_putchar('\n');
	}

}
